[package]
name = "crucible-extension"
description = "Runs somebody else's program under confinement and keeps talking to it."
publish.workspace = true
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# Core alone, deliberately. What an extension is, what it says and the sandbox
# it runs under are all domain types; a host that named a concrete backend or a
# settings document here would be deciding what it is only supposed to run.
[dependencies]
crucible-core.workspace = true

[lints]
workspace = true
