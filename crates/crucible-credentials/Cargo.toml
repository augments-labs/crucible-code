[package]
name = "crucible-credentials"
description = "Credential application and outgoing-representation redaction contracts."
publish.workspace = true
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# `sha2` derives the opaque scope identity an API key is cached under, so the
# key itself never reaches cache metadata. `thiserror` writes the module-owned
# error enum.
[dependencies]
crucible-types.workspace = true
sha2.workspace = true
thiserror.workspace = true

[lints]
workspace = true
