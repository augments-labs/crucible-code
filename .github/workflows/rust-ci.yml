name: native-sandbox-experiment
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  macos-signature-calibration:
    name: macOS self signature effect calibration (Intel)
    runs-on: macos-15-intel
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - run: sh .github/probes/macos-signature-calibration.sh
